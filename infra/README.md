# Infra

Infrastructure assets live here, but nothing is deployable yet.

- `docker/`: per-service Dockerfile placeholders
- `compose/`: local compose manifests
- `reverse-proxy/`: reverse proxy configuration
- `scripts/`: helper scripts for local development
